# Classifier checkpoint

Do not commit the classifier checkpoint directly to Git.

Publish the validated checkpoint as a GitHub Release asset and record:

- release URL
- file name
- SHA256
- architecture version
- feature version
- best validation accuracy
- FashionMNIST test10k accuracy

Only checkpoints with test10k accuracy greater than or equal to 93% are valid.
